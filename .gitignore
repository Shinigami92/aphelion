node_modules/
dist/
.DS_Store
*.log

# Intermediate downloads from scripts/fetch-assets.ts (huge source GeoTIFFs).
# The *derived* textures in public/textures are committed on purpose so the
# app runs fully offline.
.cache/

# Playwright MCP scratch output (page snapshots, screenshots).
.playwright-mcp/

# Verification screenshots dropped at the repo root. Playwright MCP is
# configured to write into .playwright-mcp/, but a server started before that
# config landed still writes here. No real asset lives at the root.
/*.png
